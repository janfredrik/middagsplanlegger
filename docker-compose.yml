version: "3.8"
services:
  middagsplanlegger:
    image: ghcr.io/janfredrik/middagsplanlegger:latest
    container_name: middagsplanlegger
    restart: unless-stopped
    ports:
      - "3200:8090"
    volumes:
      - /sti/til/data:/pb/pb_data
